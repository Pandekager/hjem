---
phase: 05-integration-polish
plan: 01
type: execute
wave: 1
depends_on: [04-frontend-tile-01]
files_modified:
  - "server/utils/systemd.ts"
  - "server/api/mc/kill.post.ts"
  - "app/components/ServerTile.vue"
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03]
---

<objective>
Add crash detection (periodic background poll) and stop timeout with force kill fallback.

POLISH-01 already done (polling after start/stop from Phase 4 fix).
POLISH-02: Background crash monitor detects unexpected server stops.
POLISH-03: If stop exceeds 60s, offer force kill button.
</objective>

<tasks>

<task type="auto">
<name>task 1: add killUnit + /api/mc/kill endpoint</name>
<files>server/utils/systemd.ts, server/api/mc/kill.post.ts</files>
<action>
Add `killUnit(unitName, signal)` to systemd.ts that calls KillUnit with SIGKILL.
Create server/api/mc/kill.post.ts that calls killUnit.
</action>
</task>

<task type="auto">
<name>task 2: crash detection + force kill in ServerTile.vue</name>
<files>app/components/ServerTile.vue</files>
<action>
Add continuous crash monitor: every 10s while server is active, check if state changes unexpectedly.
Add stop timeout: after 60s of stopping, show "Force Stop" button that calls /api/mc/kill.
</action>
</task>

</tasks>
